2---
const { title, url, image, description } = Astro.props;
import Prose from "./Prose.astro";
import "@fontsource/sarabun";
import "@fontsource-variable/jost";
import "@fontsource-variable/josefin-sans";
---

  <div class="flex space-x-4 space-y-4 mb-14">
    <div class="flex w-1/3 object-center">
      <a
        href={url}
      >
        <img src={image} class="object-cover" />
      </a>
    </div>
    <div class="w-2/3 items-center flex flex-col">
      <a
        class="prose prose-slate dark:prose-invert uppercase font-josefin no-underline"
        href={url}
      >
        <h3 class="font-josefin">{title}</h3>
        <p class="font-jost normal-case">{description}</p>
      </a>
    </div>
  </div>
